{
  "executors": {
    "release": {
      "implementation": "./release/release",
      "schema": "./release/schema.json",
      "description": "Release executor with support for single project or all projects (use releaseAll: true)"
    }
  }
}
