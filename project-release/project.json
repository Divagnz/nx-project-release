{
  "name": "project-release",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "project-release/src",
  "projectType": "library",
  "release": {
    "version": {
      "manifestRootsToUpdate": ["dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": [],
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/project-release",
        "main": "project-release/src/index.ts",
        "tsConfig": "project-release/tsconfig.lib.json",
        "assets": [
          "project-release/*.md",
          {
            "input": "./project-release/src",
            "glob": "**/!(*.ts)",
            "output": "./src"
          },
          {
            "input": "./project-release/src",
            "glob": "**/*.d.ts",
            "output": "./src"
          },
          {
            "input": "./project-release",
            "glob": "generators.json",
            "output": "."
          },
          {
            "input": "./project-release",
            "glob": "executors.json",
            "output": "."
          }
        ]
      }
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "project-release/jest.config.ts"
      }
    }
  }
}
